# toedoeloe
To-do app as Learning Opportunity for Erick


## setup
1. Make sure you have docker running. Then run from the top directory: `docker-compose up -d --build`
2. In a browser, go to localhost:3000